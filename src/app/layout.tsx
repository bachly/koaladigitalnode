import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Koala Digital | Sydney-based Marketing Assistant",
  description: "Assiting Sydney-based Small Businesses in Digital Marketing, Product Photography, Running Online Ads, Website Improvements, etc. through Custom Service & DIY Marketing Vault",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
